# Bedrock Knowledge Base
KB_ID=ABCDEG  # Knowledge Base ID (not ARN) — found in Bedrock console under Knowledge Bases

# AWS
AWS_REGION=us-east-1

# Option 1: use a named profile from ~/.aws/credentials
#AWS_PROFILE=default

# Option 2: explicit credentials (leave blank if using AWS_PROFILE)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=  # only needed for temporary/assumed-role credentials

# retrieve.py flywheel config (all optional — can also be passed as CLI flags)
#RETRIEVE_K=8
#RETRIEVE_SEARCH_TYPE=SEMANTIC        # SEMANTIC or HYBRID
#RETRIEVE_MRR_THRESHOLD=0.65
#RETRIEVE_JUDGE_MODEL=us.amazon.nova-pro-v1:0

# KB creation scripts (create_baseline_kb.py, create_experiment_kb.py)
S3_BUCKET=<your-s3-bucket-name>
AWS_ACCOUNT_ID=<your-12-digit-aws-account-id>
AWS_PERMISSIONS_BOUNDARY_ARN=arn:aws:iam::<boundary-account-id>:policy/PermissionBoundary_PowerUser
#KB_VECTOR_BUCKET=chunking-exp-vectors   # optional — default: chunking-exp-vectors
# PDF chunking params for create_experiment_kb.py (can also be passed as CLI flags)
#KB_PDF_MAX_TOKENS=1024
#KB_PDF_PERCENTILE=95

# Arize Phoenix Cloud (Phase 3)
PHOENIX_API_KEY=<from Phoenix Settings → API Keys>
PHOENIX_COLLECTOR_ENDPOINT=https://app.phoenix.arize.com/s/<your-workspace-slug>
PHOENIX_PROJECT=crdc-eval   # optional — defaults to "crdc-eval" if not set
